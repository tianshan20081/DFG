<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/lyVerticalStyle" >

    <ScrollView style="@style/scrollViewStyle" >

        <LinearLayout style="@style/lyVerticalStyle" >

            <LinearLayout style="@style/lyHorizontalStyle" >

                <Button
                    android:id="@+id/btnRefHandler"
                    style="@style/btnItem"
                    android:text="RefHandler" />

                <Button
                    android:id="@+id/btnHandlerHeelo"
                    style="@style/btnItem"
                    android:text="HandlerHeelo" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

</LinearLayout>