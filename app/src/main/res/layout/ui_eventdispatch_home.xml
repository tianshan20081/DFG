<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/lyVerticalStyle" >

    <ScrollView style="@style/scrollViewStyle" >

        <LinearLayout style="@style/lyVerticalStyle" >

            <LinearLayout style="@style/lyHorizontalStyle" >

                <Button
                    android:id="@+id/btnSimpleEventDispatch"
                    style="@style/btnItem"
                    android:text="SimpleEventDispatch" />

                <Button
                    android:id="@+id/btnEventDispatchView"
                    style="@style/btnItem"
                    android:text="EventDispatchView" />
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

</LinearLayout>